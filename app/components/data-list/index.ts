import { DataListContainer } from './data-list-container';
import { DataListOptions } from './data-list-options';
import { DataListControl } from './data-list-search';

export const Datalist = {
	Container: DataListContainer,
	Options: DataListOptions,
	Search: DataListControl,
};
